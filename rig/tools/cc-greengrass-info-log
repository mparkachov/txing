#!/usr/bin/env bash
set -euo pipefail

real_cc="${TXING_REAL_CC:-cc}"
args=()

for arg in "$@"; do
  case "$arg" in
    -DGG_LOG_LEVEL=GG_LOG_DEBUG)
      args+=("-DGG_LOG_LEVEL=GG_LOG_INFO")
      ;;
    *)
      args+=("$arg")
      ;;
  esac
done

exec "$real_cc" "${args[@]}"
