RecipeFormatVersion: '2020-01-25'
ComponentName: dev.txing.device.weather.SparkplugManager
ComponentVersion: '0.7.0'
ComponentType: aws.greengrass.generic
ComponentDescription: txing weather Sparkplug lifecycle manager for BLE connected-idle weather devices.
ComponentPublisher: txing
ComponentDependencies:
  aws.greengrass.TokenExchangeService:
    VersionRequirement: '>=0.0.0'
    DependencyType: HARD
ComponentConfiguration:
  DefaultConfiguration:
    RigId: REPLACE_WITH_RIG_THING_ID
    TownId: REPLACE_WITH_TOWN_THING_ID
    accessControl:
      aws.greengrass.ipc.pubsub:
        dev.txing.device.weather.SparkplugManager:pubsub:1:
          policyDescription: Allows the weather Sparkplug manager to exchange local connectivity messages with weather adapters.
          operations:
            - aws.greengrass#PublishToTopic
            - aws.greengrass#SubscribeToTopic
          resources:
            - dev/txing/rig/v1/connectivity/*
Manifests:
  - Platform:
      os: linux
      architecture: aarch64
      runtime: aws_nucleus_lite
    Lifecycle:
      run:
        Script: |-
          export RIG_NAME="{configuration:/RigId}"
          export RIG_ID="{configuration:/RigId}"
          export TXING_RIG_ID="{configuration:/RigId}"
          export TXING_TOWN_ID="{configuration:/TownId}"
          export SPARKPLUG_GROUP_ID="{configuration:/TownId}"
          export SPARKPLUG_EDGE_NODE_ID="{configuration:/RigId}"
          export TXING_DEVICE_TYPE="weather"
          export TXING_DEVICE_DIR="{artifacts:decompressedPath}/rig-greengrass/devices/weather"
          export TXING_DEVICE_MANIFEST="{artifacts:decompressedPath}/rig-greengrass/devices/weather/manifest.toml"
          export PYTHONPATH="{artifacts:decompressedPath}/rig-greengrass/python"
          exec python3 -m weather_rig.sparkplug_manager
    Artifacts:
      - Uri: s3://REPLACE_WITH_TXING_GREENGRASS_ARTIFACT_BUCKET/rig-greengrass/0.7.0/rig-greengrass.zip
        Unarchive: ZIP
