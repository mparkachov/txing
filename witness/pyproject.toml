[project]
name = "witness"
version = "0.1.0"
description = "Sparkplug witness projection for txing"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.34,<2",
]

[build-system]
requires = ["uv_build>=0.10.6,<0.11.0"]
build-backend = "uv_build"
