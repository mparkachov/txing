set shell := ["bash", "-eu", "-o", "pipefail", "-c"]
set quiet := true

witness_dir := source_directory()

@test:
    cd "{{witness_dir}}" && go test -tags lambda.norpc ./...

deploy:
    @echo "Direct per-function deploy is disabled. Use the release workflow and just aws::publish-lambda latest." >&2
    @exit 1

@default:
    @just --list
