[project]
name = "time-device"
version = "0.5.0"
description = "AWS Lambda runtime for the txing virtual time device"
requires-python = ">=3.12"
dependencies = [
    "boto3>=1.34,<2",
]

[build-system]
requires = ["uv_build>=0.10.6,<0.11.0"]
build-backend = "uv_build"
