#ifndef TXING_BOARD_KVS_MASTER_BOARD_VIDEO_PROTO_BINDING_HPP
#define TXING_BOARD_KVS_MASTER_BOARD_VIDEO_PROTO_BINDING_HPP

// Generated by CMake from the device profile. The BoardVideoBridge service name
// is already device-independent, but its proto package is still per device, so
// the generated header path and namespace are resolved here and the shared
// sources only refer to txing::board::proto::board_video_v1.
#include "txing/@TXING_BOARD_DEVICE_TYPE@/board_video/v1/board_video.grpc.pb.h"

namespace txing::board::proto {
namespace board_video_v1 = ::txing::@TXING_BOARD_DEVICE_TYPE@::board_video::v1;
}  // namespace txing::board::proto

#endif
