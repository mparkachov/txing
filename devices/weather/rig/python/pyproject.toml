[project]
name = "weather-rig"
version = "0.6.0"
description = "Weather device-type runtime adapter for txing Raspberry Pi rigs"
requires-python = ">=3.12"
dependencies = [
    "awsiotsdk>=1.22,<2",
    "bleak>=0.22,<1",
    "boto3>=1.34,<2",
    "aws",
    "rig",
]

[project.scripts]
weather-rig = "weather_rig:main"
weather-rig-connectivity-ble = "weather_rig.connectivity_ble:main"
weather-rig-sparkplug-manager = "weather_rig.sparkplug_manager:main"

[build-system]
requires = ["uv_build>=0.10.6,<0.11.0"]
build-backend = "uv_build"

[tool.uv.sources]
aws = { path = "../../../../shared/aws/python" }
rig = { path = "../../../../rig" }
