type = "weather"
device_name = "outside"
display_name = "Weather"
capabilities = ["sparkplug"]
compatible_rig_types = ["raspi"]

[shadows.sparkplug]
schema = "aws/sparkplug-shadow.schema.json"
default = "aws/default-sparkplug-shadow.json"

[[rig.processes]]
name = "weather-matter-watch"
command = "uv"
args = ["run", "--project", "rig/python", "weather-rig-matter-watch"]
cwd = "."
environment = [
  "TXING_DEVICE_TYPE",
  "TXING_DEVICE_DIR",
  "TXING_DEVICE_MANIFEST",
  "RIG_NAME",
  "THING_NAME",
  "SPARKPLUG_GROUP_ID",
  "SPARKPLUG_EDGE_NODE_ID",
  "AWS_REGION",
  "WEATHER_THING_NAME",
  "WEATHER_MATTER_NODE_ID",
  "WEATHER_CHIP_TOOL",
]

[[rig.processes]]
name = "weather-sparkplug-manager"
command = "uv"
args = ["run", "--project", "rig/python", "weather-rig-sparkplug-manager"]
cwd = "."
environment = [
  "TXING_DEVICE_TYPE",
  "TXING_DEVICE_DIR",
  "TXING_DEVICE_MANIFEST",
  "RIG_NAME",
  "THING_NAME",
  "SPARKPLUG_GROUP_ID",
  "SPARKPLUG_EDGE_NODE_ID",
  "AWS_REGION",
]

[web]
adapter = "web/weather-adapter.tsx"
