# Template Rig Component

This folder is only an example location. Rig-side connectivity should be
packaged as a Greengrass component that implements the generic v2 capability
contract used by `dev.txing.rig.SparkplugManager`.
