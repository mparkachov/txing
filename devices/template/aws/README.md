# Template AWS Assets

Place device-type shadow schemas and default shadow payloads here.

Each capability declared in `manifest.toml` must have a matching `[shadows.<name>]`
entry that points at files in this directory.
