set shell := ["bash", "-eu", "-o", "pipefail", "-c"]
set quiet := true

lambda_dir := source_directory()

@test:
    cd "{{lambda_dir}}" && go test -tags lambda.norpc ./...

build:
    #!/usr/bin/env bash
    set -euo pipefail
    cd "{{lambda_dir}}"
    GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -trimpath -tags lambda.norpc -ldflags="-s -w" -o target/release/txing-cloud-rig-lambda ./cmd/txing-cloud-rig-lambda
    GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -trimpath -tags lambda.norpc -ldflags="-s -w" -o target/release/txing-cloud-mcu-lambda ./cmd/txing-cloud-mcu-lambda

deploy:
    @echo "Direct per-function deploy is disabled. Use: just aws::deploy-local-lambda txing-cloud-mcu-lambda" >&2
    @exit 1

@default:
    @just --list
