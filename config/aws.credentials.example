# Copy this file to `config/aws.credentials`.
# Fill in the source profile named by AWS_SOURCE_PROFILE in `config/aws.env`.
# Do not store generated role-assumption profiles in this repository.

[town]
aws_access_key_id = REPLACE_ME
aws_secret_access_key = REPLACE_ME
